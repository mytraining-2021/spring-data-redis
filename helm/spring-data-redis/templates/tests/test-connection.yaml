apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-data-redis.fullname" . }}-test-connection"
  labels:
    {{- include "spring-data-redis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-data-redis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
